/*10 – DESENVOLVA UM PROGRAMA EM JAVASCRIPT ONDE O USUÁRIO IRÁ DIGITAR AS INFORMAÇÕES DE 5
CARROS. O CARRO POSSUI OS ATRIBUTOS MODELO, FABRICANTE, ANO E VALOR. O SISTEMA APÓS A INSERÇÃO 
DOS CARROS PELO USUÁRIO DEVERÁ:
- CONTABILIZAR O TOTAL (VALOR) DOS CARROS;
- LISTAR OS CARROS COM ANO DE FABRICAÇÃO ABAIXO DE UM DETERMINADO ANO INFORMADO PELO 
USUÁRIO;
- LISTAR OS CARROS DE UM DETERMINADO FABRICANTE INFORMADO PELO USUÁRIO*/ 

const teclado = require('prompt-sync')();

let carros = [];

for(let i = 0; i<3; i++){

    let carro = {
        modelo : modelo,
        fabricante : fabricante,
    }
}

let acumulador = 0;

for(let carro of carros){
    acumulador += carro.valor;
}
console.log('Valor total dos carros:');

function listaCarrosAbaixo(ano){
    for(let carro of carros){
        if(carr)
    }
}